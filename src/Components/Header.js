import Button  from'./Button'
const Header = () => {

    return(
        <header>
            <h1>Task Tracker</h1>
            <Button color = 'red' text = 'Hello' />
            <Button text = 'Hello world'/>
        </header>
    )
}

export default Header